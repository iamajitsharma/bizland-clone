(function () {
  "use strict";

  window.addEventListener("load", () => {
    AOS.init({
      duration: 1200,
      easing: "ease-in-out",
      once: "true",
      mirror: false,
    });
  });
})();
